// Copy this file to secrets.h and fill in your own values.
// secrets.h is gitignored so credentials never reach the repository.
#pragma once

#define WIFI_SSID       "YOUR_WIFI_NAME"      // 2.4 GHz only
#define WIFI_PASSWORD   "YOUR_WIFI_PASSWORD"

// Firebase > Project settings > General > Web API Key
#define API_KEY         "YOUR_FIREBASE_WEB_API_KEY"

// Firebase > Realtime Database > URL shown above the data tree
#define DATABASE_URL    "https://YOUR-PROJECT-default-rtdb.REGION.firebasedatabase.app"

// Must match the Device ID entered in the app's pairing sheet
#define DEVICE_ID       "hydro-smart-01"
